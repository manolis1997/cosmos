[project]
name = "cosmos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "databricks-connect>=18.1.2",
    "fastapi>=0.135.3",
    "pyyaml>=6.0.3",
]

[build-system]
requires = ["setuptools"]

[tool.setuptools.packages.find]
where = ["."]
exclude = [".venv*", "tests*"]

